	.code
	.align 4
	.level 2.0
	sub,* %r2,%r3,%r4
	sub,*= %r2,%r3,%r4
	sub,*< %r2,%r3,%r4
	sub,*<= %r2,%r3,%r4
	sub,*<< %r2,%r3,%r4
	sub,*<<= %r2,%r3,%r4
	sub,*sv %r2,%r3,%r4
	sub,*od %r2,%r3,%r4
	sub,*tr %r2,%r3,%r4
	sub,*<> %r2,%r3,%r4
	sub,*>= %r2,%r3,%r4
	sub,*> %r2,%r3,%r4
	sub,*>>= %r2,%r3,%r4
	sub,*>> %r2,%r3,%r4
	sub,*nsv %r2,%r3,%r4
	sub,*ev %r2,%r3,%r4
	sub,tc,* %r2,%r3,%r4
	sub,tc,*= %r2,%r3,%r4
	sub,tc,*< %r2,%r3,%r4
	sub,tc,*<= %r2,%r3,%r4
	sub,tc,*<< %r2,%r3,%r4
	sub,tc,*<<= %r2,%r3,%r4
	sub,tc,*sv %r2,%r3,%r4
	sub,tc,*od %r2,%r3,%r4
	sub,tc,*tr %r2,%r3,%r4
	sub,tc,*<> %r2,%r3,%r4
	sub,tc,*>= %r2,%r3,%r4
	sub,tc,*> %r2,%r3,%r4
	sub,tc,*>>= %r2,%r3,%r4
	sub,tc,*>> %r2,%r3,%r4
	sub,tc,*nsv %r2,%r3,%r4
	sub,tc,*ev %r2,%r3,%r4
	sub,db,* %r2,%r3,%r4
	sub,db,*= %r2,%r3,%r4
	sub,db,*< %r2,%r3,%r4
	sub,db,*<= %r2,%r3,%r4
	sub,db,*<< %r2,%r3,%r4
	sub,db,*<<= %r2,%r3,%r4
	sub,db,*sv %r2,%r3,%r4
	sub,db,*od %r2,%r3,%r4
	sub,db,*tr %r2,%r3,%r4
	sub,db,*<> %r2,%r3,%r4
	sub,db,*>= %r2,%r3,%r4
	sub,db,*> %r2,%r3,%r4
	sub,db,*>>= %r2,%r3,%r4
	sub,db,*>> %r2,%r3,%r4
	sub,db,*nsv %r2,%r3,%r4
	sub,db,*ev %r2,%r3,%r4
	sub,db,* %r2,%r3,%r4
	sub,db,*= %r2,%r3,%r4
	sub,db,*< %r2,%r3,%r4
	sub,db,*<= %r2,%r3,%r4
	sub,db,*<< %r2,%r3,%r4
	sub,db,*<<= %r2,%r3,%r4
	sub,db,*sv %r2,%r3,%r4
	sub,db,*od %r2,%r3,%r4
	sub,db,*tr %r2,%r3,%r4
	sub,db,*<> %r2,%r3,%r4
	sub,db,*>= %r2,%r3,%r4
	sub,db,*> %r2,%r3,%r4
	sub,db,*>>= %r2,%r3,%r4
	sub,db,*>> %r2,%r3,%r4
	sub,db,*nsv %r2,%r3,%r4
	sub,db,*ev %r2,%r3,%r4
	sub,tsv,* %r2,%r3,%r4
	sub,tsv,*= %r2,%r3,%r4
	sub,tsv,*< %r2,%r3,%r4
	sub,tsv,*<= %r2,%r3,%r4
	sub,tsv,*<< %r2,%r3,%r4
	sub,tsv,*<<= %r2,%r3,%r4
	sub,tsv,*sv %r2,%r3,%r4
	sub,tsv,*od %r2,%r3,%r4
	sub,tsv,*tr %r2,%r3,%r4
	sub,tsv,*<> %r2,%r3,%r4
	sub,tsv,*>= %r2,%r3,%r4
	sub,tsv,*> %r2,%r3,%r4
	sub,tsv,*>>= %r2,%r3,%r4
	sub,tsv,*>> %r2,%r3,%r4
	sub,tsv,*nsv %r2,%r3,%r4
	sub,tsv,*ev %r2,%r3,%r4
	sub,tsv,tc,* %r2,%r3,%r4
	sub,tsv,tc,*= %r2,%r3,%r4
	sub,tsv,tc,*< %r2,%r3,%r4
	sub,tsv,tc,*<= %r2,%r3,%r4
	sub,tsv,tc,*<< %r2,%r3,%r4
	sub,tsv,tc,*<<= %r2,%r3,%r4
	sub,tsv,tc,*sv %r2,%r3,%r4
	sub,tsv,tc,*od %r2,%r3,%r4
	sub,tsv,tc,*tr %r2,%r3,%r4
	sub,tsv,tc,*<> %r2,%r3,%r4
	sub,tsv,tc,*>= %r2,%r3,%r4
	sub,tsv,tc,*> %r2,%r3,%r4
	sub,tsv,tc,*>>= %r2,%r3,%r4
	sub,tsv,tc,*>> %r2,%r3,%r4
	sub,tsv,tc,*nsv %r2,%r3,%r4
	sub,tsv,tc,*ev %r2,%r3,%r4
	sub,db,tsv,* %r2,%r3,%r4
	sub,db,tsv,*= %r2,%r3,%r4
	sub,db,tsv,*< %r2,%r3,%r4
	sub,db,tsv,*<= %r2,%r3,%r4
	sub,db,tsv,*<< %r2,%r3,%r4
	sub,db,tsv,*<<= %r2,%r3,%r4
	sub,db,tsv,*sv %r2,%r3,%r4
	sub,db,tsv,*od %r2,%r3,%r4
	sub,db,tsv,*tr %r2,%r3,%r4
	sub,db,tsv,*<> %r2,%r3,%r4
	sub,db,tsv,*>= %r2,%r3,%r4
	sub,db,tsv,*> %r2,%r3,%r4
	sub,db,tsv,*>>= %r2,%r3,%r4
	sub,db,tsv,*>> %r2,%r3,%r4
	sub,db,tsv,*nsv %r2,%r3,%r4
	sub,db,tsv,*ev %r2,%r3,%r4
	sub,db,tsv,* %r2,%r3,%r4
	sub,db,tsv,*= %r2,%r3,%r4
	sub,db,tsv,*< %r2,%r3,%r4
	sub,db,tsv,*<= %r2,%r3,%r4
	sub,db,tsv,*<< %r2,%r3,%r4
	sub,db,tsv,*<<= %r2,%r3,%r4
	sub,db,tsv,*sv %r2,%r3,%r4
	sub,db,tsv,*od %r2,%r3,%r4
	sub,db,tsv,*tr %r2,%r3,%r4
	sub,db,tsv,*<> %r2,%r3,%r4
	sub,db,tsv,*>= %r2,%r3,%r4
	sub,db,tsv,*> %r2,%r3,%r4
	sub,db,tsv,*>>= %r2,%r3,%r4
	sub,db,tsv,*>> %r2,%r3,%r4
	sub,db,tsv,*nsv %r2,%r3,%r4
	sub,db,tsv,*ev %r2,%r3,%r4
